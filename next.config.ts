/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com'], // Menambahkan domain yang diizinkan
        domains: ['i.imgur.com'],
    },
}

module.exports = nextConfig

